<!DOCTYPE html>
<html>
<head>
  <title>HomeCanvasr</title>
  <%= stylesheet_link_tag    "application" ,:media => "all" %>
  <%= javascript_include_tag "application"  %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="container">

    <%= render "layouts/header" %>

    <% flash.each do |name, msg| %>
      <div class="alert alert-info"> <%= content_tag :div, msg, id: "flash_#{name}" %> </div>
    <% end %>

    <%= yield %>
  
  </div>
</body>
</html>

